# Base image
FROM node:20-alpine

# Working directory
WORKDIR /app

# Install dependencies first (for caching)
COPY scripts/package.json scripts/package-lock.json* ./
RUN npm install

# Copy application source
COPY scripts/draw.js .

# Default command (can be overridden)
CMD ["node", "draw.js"]
